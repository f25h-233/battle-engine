[project]
name = "battle-engine"
version = "0.2.0"
description = "D&D 5e combat engine — resolution core + CLI"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[project.optional-dependencies]
web = ["flask>=3.0"]
